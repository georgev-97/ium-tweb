<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

        <item android:id="@+id/view"
            android:title="Prenotazioni"
            android:orderInCategory="100"
            android:icon="@drawable/view"
            app:showAsAction = "always"
            android:titleCondensed="Vedi prenotazioni"/>

        <item android:id="@+id/add"
            android:title="Prenota"
            android:orderInCategory="100"
            android:icon="@drawable/add"
            app:showAsAction = "always"
            android:titleCondensed="Aggiungi prenotazione"/>

    <item android:id="@+id/logout"
        android:title="Esci"
        android:orderInCategory="100"
        app:showAsAction = "always"
        android:icon="@drawable/logout"
        android:titleCondensed="Esci"/>

</menu>
